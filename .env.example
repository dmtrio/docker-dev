# .env.example — reference for ~/dev-agent/secrets.env
#
# secrets.env holds EVERY secret value in one place: chmod 600, never
# mounted into containers. up.sh composes per-container credentials from
# it according to what each containers/<name>.yml grants.
#
#   touch ~/dev-agent/secrets.env && chmod 600 ~/dev-agent/secrets.env
#   then add lines like the ones below (or drop key files into
#   ~/dev-agent/shared/secrets/ and run ./migrate-secrets.sh)

# ── Service tokens ────────────────────────────────────────────────────────────
# Self-generated on the first run of each run-*.sh — no need to create these
# by hand. Shown here only so you know what they are.
#MCP_GATEWAY_TOKEN=<generated by run-gateway-coding.sh>
#PROXYMAN_BRIDGE_KEY=<generated by run-proxyman-bridge.sh>
#RESEARCH_BROWSER_KEY=<generated by run-research-browser.sh>

# ── GitHub machine user ──────────────────────────────────────────────────────
# Fine-grained PAT from the machine account (NOT your personal account):
# Contents RW, Pull requests RW, Issues RW, Actions R, Checks R. No Workflows.
# Agents act as this identity; your own gh login stays in shared/gh.
GH_TOKEN=github_pat_xxxxxxxxxxxx

# ── Obsidian Annotated identities ────────────────────────────────────────────
# One key per container+agent pair, minted in Settings → Annotated.
# Convention: OBSIDIAN_KEY_<container>_<agent>, dashes become underscores.
# Additive tier for normal agents; destructive only for vault maintenance;
# poll-tier keys (watchers) use the OBSIDIAN_WATCH_KEY_ prefix.
#
# These examples match containers/TEMPLATE.yml's example identities:
OBSIDIAN_KEY_personal_site_claude=ann_additive_xxxxxxxx
OBSIDIAN_KEY_personal_site_codex=ann_additive_xxxxxxxx
OBSIDIAN_WATCH_KEY_personal_site_claude=ann_poll_xxxxxxxx

# ══════════════════════════════════════════════════════════════════════════════
# HOMELAB ONLY: the macvlan launcher (new-container.sh) reads a local .env
# with your SSH public key. Not used by the local/attach setup at all.
#AUTHORIZED_KEY="ssh-ed25519 AAAA... your@machine"
