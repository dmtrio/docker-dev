# .env.example — copy to .env and fill in values
# This is the global config used by new-container.sh

# Your SSH public key (from ~/.ssh/id_ed25519.pub or similar)
AUTHORIZED_KEY="ssh-ed25519 AAAA... your@machine"

# Git identity
GIT_USER_NAME=Demetrio
GIT_USER_EMAIL=your@email.com
