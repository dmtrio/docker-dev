# annotated-watch — env-only plugin: an agent-scoped secret slot, no MCP server.
# Delivers ANNOTATED_WATCH_KEY (the watch-vault poll key) per agent. See ./README.md.

secrets:
  ANNOTATED_WATCH_KEY: agent
